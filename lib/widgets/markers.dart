import 'package:flutter/material.dart';

class HomeBaseMarker extends StatelessWidget {
  const HomeBaseMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
