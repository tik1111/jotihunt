import 'package:flutter/material.dart';

class AddMarkerToMap extends StatelessWidget {
  const AddMarkerToMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
